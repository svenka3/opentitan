```sh
sudo yum install -y install autoconf bison build-essential clang-format curl     
sudo yum install -y doxygen flex g++ git libelf1 libelf-dev libftdi1-2 libftdi1-dev libssl-dev     l
sudo yum install -y ibusb-1.0-0 lsb-release make ninja-build pkgconf 
sudo yum install -y python3 python3-pip     python3-setuptools python3-wheel python3-yaml 
sudo yum install -y srecord tree xsltproc     zlib1g-dev xz-utils

sudo yum install -y openssl11-libs openssl11-devel epel-release

sudo pip3 install -r python-requirements.txt

(Or pip3 install --user -r python-requirements.txt)
```



